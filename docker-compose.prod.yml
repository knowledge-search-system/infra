services:

  postgres:
    image: pgvector/pgvector:pg16

    restart: unless-stopped

    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: postgres

    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d

    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER}" ]
      interval: 5s
      timeout: 5s
      retries: 10

    networks:
      - knowledge-search-network

  redis:
    image: redis:7-alpine

    restart: unless-stopped

    volumes:
      - redis_data:/data

    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 5s
      retries: 10

    networks:
      - knowledge-search-network

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.18.1

    restart: unless-stopped

    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      ES_JAVA_OPTS: "-Xms512m -Xmx512m"

    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data

    networks:
      - knowledge-search-network

  search-engine-migrate:
    image: ghcr.io/knowledge-search-system/search-engine-migrate:main

    restart: "no"

    depends_on:
      postgres:
        condition: service_healthy

    environment:
      DATABASE_DSN: ${SEARCH_ENGINE_POSTGRES_DSN}

    networks:
      - knowledge-search-network

  search-engine:
    image: ghcr.io/knowledge-search-system/search-engine:main

    restart: unless-stopped

    env_file:
      - .env

    depends_on:
      search-engine-migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      elasticsearch:
        condition: service_started

    ports:
      - "8080:8080"

    networks:
      - knowledge-search-network

  document-processor-migrate:
    image: ghcr.io/knowledge-search-system/document-processor-migrate:main

    restart: "no"

    depends_on:
      postgres:
        condition: service_healthy

    environment:
      DATABASE_DSN: ${DOCUMENT_PROCESSOR_POSTGRES_DSN}

    networks:
      - knowledge-search-network

  document-processor:
    image: ghcr.io/knowledge-search-system/document-processor:main

    restart: unless-stopped

    env_file:
      - .env

    depends_on:
      document-processor-migrate:
        condition: service_completed_successfully
      search-engine:
        condition: service_started

    ports:
      - "8081:8081"

    networks:
      - knowledge-search-network

  ml-service-migrate:
    image: ghcr.io/knowledge-search-system/ml-serivce-migrate:main

    restart: "no"

    depends_on:
      postgres:
        condition: service_healthy

    environment:
      ML_SERVICE_POSTGRES_DSN: ${ML_SERVICE_POSTGRES_DSN}

    networks:
      - knowledge-search-network

  ml-service:
    image: ghcr.io/knowledge-search-system/ml-serivce:main

    restart: unless-stopped

    env_file:
      - .env

    depends_on:
      ml-service-migrate:
        condition: service_completed_successfully

    networks:
      - knowledge-search-network

networks:
  knowledge-search-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  elasticsearch_data:
